# Backend Environment Variables
# Bu dosyayı kopyalayıp .env olarak kaydedin

# === Google Gemini API ===
GOOGLE_API_KEY=your_api_key_here

# === Admin Token (/api/update-data endpoint'ini korur) ===
ADMIN_SECRET_TOKEN=your_secret_token_here

# === Environment ===
# Options: development, production
ENVIRONMENT=development

# === Logging ===
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# === Features ===
USE_EMBEDDINGS=true

# === Opsiyonel: Hava Durumu ===
# OPENWEATHER_API_KEY=your_openweather_api_key

# === Opsiyonel: Sentry Hata İzleme ===
# SENTRY_DSN=your_sentry_dsn

# === Opsiyonel: CORS ===
# ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# === Opsiyonel: Gemini Model ===
# GEMINI_MODEL=gemini-1.5-flash

# === Opsiyonel: Redis Cache ===
# REDIS_URL yoksa process-içi dict cache kullanılır (restart'ta sıfırlanır)
# REDIS_URL=redis://localhost:6379/0
